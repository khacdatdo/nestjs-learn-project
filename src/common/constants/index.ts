// roles
export const ROLES = {
  ADMIN: 1,
  MOD: 2,
  MEMBER: 3,
};
export const DEFAULT_ROLE = ROLES.MEMBER;

// default min confession length
export const MIN_CONFESSION_LENGTH = 300;

// default min and max of sender name length
export const MIN_SENDER_NAME_LENGTH = 5;
export const MAX_SENDER_NAME_LENGTH = 200;
